import {MessageFlags} from "discord.js";import {db} from "../database/mysql.js";import {getDriver} from "../drivers/service.js";export async function createLoa(i){const d=await getDriver(i.user.id);if(!d)return i.reply({content:"Driver profile required.",flags:MessageFlags.Ephemeral});await db().execute("INSERT INTO loa_requests(driver_id,start_date,expected_return_date,reason) VALUES(?,?,?,?)",[d.id,i.options.getString("start",true),i.options.getString("return",true),i.options.getString("reason",true)]);await i.reply({content:"✅ LOA request submitted.",flags:MessageFlags.Ephemeral});}
