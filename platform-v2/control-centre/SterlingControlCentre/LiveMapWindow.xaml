<Window x:Class="Sterling.Logistics.ControlCentre.LiveMapWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
 Title="Sterling Live Map" Width="1400" Height="850" MinWidth="1000" MinHeight="650"
 WindowStartupLocation="CenterOwner" Background="{StaticResource Bg}">
 <Grid Margin="18">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <DockPanel Margin="0,0,0,14">
   <StackPanel DockPanel.Dock="Left">
    <TextBlock Text="LIVE FLEET MAP" Foreground="White" FontSize="28" FontWeight="Bold"/>
    <TextBlock Text="Authenticated Sterling drivers with live coordinates" Foreground="{StaticResource Muted}"/>
   </StackPanel>
   <Button DockPanel.Dock="Right" Content="Refresh Now" Width="120" Height="36" VerticalAlignment="Center" Click="RefreshButton_Click"/>
  </DockPanel>
  <Border Grid.Row="1" Background="{StaticResource Panel}" CornerRadius="12" ClipToBounds="True">
   <wv2:WebView2 x:Name="MapView"/>
  </Border>
  <TextBlock Grid.Row="2" x:Name="MapStatusText" Foreground="{StaticResource Muted}" Margin="0,12,0,0" Text="Preparing live map..."/>
 </Grid>
</Window>
